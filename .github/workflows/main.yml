name: Deploy to Server (Simple)

on:
  push:
    branches: [deploy-tests]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Test SSH Connection
      run: |
        ssh -i ~/.ssh/techlifer-linode-github-vasilymac -o StrictHostKeyChecking=no vasily@${{ secrets.HOST }} "echo 'SSH connection successful'"

    - name: Deploy to Test Server
      if: github.ref == 'refs/heads/deploy-tests'
      run: |
        echo "Deploying to test server..."
        # Add your test deployment steps here

    - name: Deploy to Production Server
      if: github.ref == 'refs/heads/master'
      run: |
        echo "Deploying to production server..."
        # Add your production deployment steps here